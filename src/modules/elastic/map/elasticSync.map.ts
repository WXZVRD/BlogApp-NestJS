import {ITypedSyncMap} from "../types/map-tables.type";

export const ESSyncMap: ITypedSyncMap = [
    {
        tableName: 'review',
        fields: [
            {
                name: 'title',
                type: 'string'
            }
        ]
    }
]